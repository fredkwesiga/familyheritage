# ---------------------------------------------------------------------------
# Root .env - consumed by docker-compose.yml ONLY.
# Copy to .env:  cp .env.example .env
# Application secrets live in apps/api/.env (see apps/api/.env.example).
# ---------------------------------------------------------------------------

POSTGRES_USER=fh
POSTGRES_PASSWORD=fh_dev_password
POSTGRES_DB=family_heritage
POSTGRES_PORT=5432
